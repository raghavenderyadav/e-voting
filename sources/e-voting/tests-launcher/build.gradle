apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile(project(":master-client"))
    compile(project(":client"))
    compile(project(":registries-server"))
    compile(project(":results-builder"))
}