'use strict';

describe('e-voting.voting.voting-question-view module', function() {

  beforeEach(module('e-voting.voting.voting-question-view'));

  describe('VotingController controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var VotingController = $controller('VotingController');
      expect(VotingController).toBeDefined();
    }));

  });
});