'use strict';

describe('e-voting.voting.voting-list-view module', function() {

  beforeEach(module('e-voting.voting.voting-list-view'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var VotingListController = $controller('VotingListController');
      expect(VotingListController).toBeDefined();
    }));

  });
});