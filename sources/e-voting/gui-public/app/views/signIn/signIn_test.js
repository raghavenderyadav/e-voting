'use strict';

describe('e-voting.signIn.signIn-view module', function() {

  beforeEach(module('e-voting.signIn.signIn-view'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SignInController = $controller('SignInController');
      expect(SignInController).toBeDefined();
    }));

  });
});