'use strict';

describe('e-voting.voting.voting-result-view module', function() {

  beforeEach(module('e-voting.voting.voting-result-view'));

  describe('VotingResultController controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var VotingResultController = $controller('VotingResultController');
      expect(VotingResultController).toBeDefined();
    }));

  });
});